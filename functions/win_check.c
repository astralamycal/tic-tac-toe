// insert imports here
#include <stdlib.h>
#include <stdio.h>

// main script
int win_check(char **board, int choice)
{
    // if (board[choice + 3] == 'X' && board[choice + 6] == 'X') // collumn check for the user
    //{
    //     return (1);
    // }
}